import React from "react";
import { View, SafeAreaView, Text } from "react-native";

function RnSafeAreaView(props) {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Text>Page content</Text>
    </SafeAreaView>
  );
}

export default RnSafeAreaView;
